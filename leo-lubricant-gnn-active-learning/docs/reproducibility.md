# Reproducibility

Reproducibility controls included in the repository:

- fixed random seeds through `leo_lubricants.utils.seed`
- YAML-driven configuration files under `configs/`
- versioned outputs under `results/`
- package-level tests under `tests/`

Reproducibility for scientific conclusions still depends on validated datasets, environment tracking, and robust experimental records.

